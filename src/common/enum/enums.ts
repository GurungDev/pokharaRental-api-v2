export enum RequestDataPaths {
    Body,
    Params,
    Query,
    Files,
  }

export enum UserEnum {
    STORE= "store",
    CUSTOMER= "customer",
    ADMIN= "admin",
}

export enum UserServiceEnum {
  STORE= "storeService",
  CUSTOMER= "customerService",
  ADMIN= "adminService",
}